import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import reportWebVitals from './reportWebVitals';

const body = ReactDOM.createRoot(document.querySelector('body'));
body.render(
  <React.StrictMode>
      {/*компоненты зашружать сюда <App><App/></App>*/}
  </React.StrictMode>
);


reportWebVitals();
